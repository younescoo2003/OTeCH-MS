services:
  redis:
    container_name: "otech-redis"
    image: redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

#  celery_worker:
#    container_name: "otech-celery_worker"
#    command: celery -A config worker --loglevel=info
#    volumes:
#      - .:/app
#    depends_on:
#      - redis

volumes:
  redis_data:
